// Prompt 2.1 stub — imports db.ts to trigger startup schema introspection.
// Full HTTP + socket.io server scaffolded in Prompt 2.2.
import './db'

console.log('[ruflo-api] server stub ready — awaiting Prompt 2.2')
